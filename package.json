{
  "name": "@sandersland/csv",
  "version": "0.2.0",
  "description": "simple module for working with csv files",
  "main": "dist/csv/index.js",
  "types": "dist/csv/index.d.ts",
  "scripts": {
    "prebuild": "node -e \"require('fs').rmdir('dist', {recursive: true}, () => {});\"",
    "build:dev": "tsc",
    "build": "tsc --project tsconfig.production.json",
    "test": "npm run build:dev && jest ./dist/tests",
    "publish": "npm run build && npm deploy"
  },
  "files": [
    "dist/csv"
  ],
  "keywords": [
    "csv",
    "table"
  ],
  "author": "Steffen Andersland",
  "devDependencies": {
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.1",
    "@babel/preset-typescript": "^7.13.0",
    "@types/jest": "^26.0.23",
    "@types/node": "^15.0.2",
    "babel-jest": "^26.6.3",
    "jest": "^26.5.0"
  }
}
